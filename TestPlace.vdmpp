class TestPlace is subclass of MyTestCase

operations
  public testConstructor: () ==> ()
  testConstructor() == (
  	dcl p : Place := new Place("test1", 1);
	  assertEqual("test1",  p.getLabel());
	  assertEqual(1,  p.getCapacity());
  );
    
  -- Entry point that runs all tests with valid inputs
  public testAll: () ==> ()
  testAll() == (
    testConstructor();
  );  
 
end TestPlace

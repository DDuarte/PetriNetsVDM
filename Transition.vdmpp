class Transition
  instance variables
    public label: seq of char;

  operations
    public Transition: seq of char ==> Transition
    Transition(newLabel) == (
      label := newLabel;
      return self;
    );
end Transition

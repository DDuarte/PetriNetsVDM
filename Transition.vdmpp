class Transition
  instance variables
		label: set of char;

	operations
	  public Transition: set of char ==> Transition
      Transition (newLabel) == (
        label := newLabel;
        return self;
      );
end Transition
